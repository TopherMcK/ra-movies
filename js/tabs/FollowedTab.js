import React from 'react';
import { Button, StyleSheet, Text, TextInput, View } from 'react-native';

export default class FollowedTab extends React.Component {
    render() {
        return (
            <Text>Followed</Text>
        );
    }
}